version: '3.8'

services:
  api:
    build: .
    environment:
      - PORT=${PORT:-8000}
      - ENV=production
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M