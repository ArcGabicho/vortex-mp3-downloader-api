version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENV=development
    volumes:
      - ./temp:/app/temp
    restart: unless-stopped